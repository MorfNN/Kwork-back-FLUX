# Backend-API для генерации изображений (Flux Provider)

Этот репозиторий содержит Backend-сервис для генерации изображений, построенный на микросервисной асинхронной архитектуре. Проект полностью готов к деплою (Docker, CI/CD) и поддерживает модульное подключение различных AI-моделей.

В качестве демонстрационного провайдера реализована интеграция с **Flux.1 (Schnell)**.

## 📚 Содержание
1. [Технологический стек](#технологический-стек)
2. [Архитектура](#архитектура)
3. [Установка и Запуск (Dev)](#установка-и-запуск-dev)
4. [Миграции БД](#миграции-бд)
5. [API Интерфейс](#api-интерфейс)
6. [Конфигурация](#конфигурация)
7. [Деплой](#деплой)

---

## 🛠 Технологический стек

*   **Язык**: Python 3.11+
*   **API**: FastAPI, Pydantic v2
*   **DB**: PostgreSQL + SQLAlchemy (Async) + Alembic
*   **Async Queue**: Celery + Redis
*   **AI Model**: HuggingFace Diffusers (`black-forest-labs/FLUX.1-schnell`)
*   **Storage**: S3 Compatible (MinIO/AWS)
*   **Infrastructure**: Docker, Nginx

---

## 🏗 Архитектура

1.  **API Service**: Принимает HTTP-запросы, валидирует данные, создает задачу в БД со статусом `QUEUED` и отправляет её в Redis.
2.  **Celery Worker**: Фоновый процесс. Забирает задачу из Redis, инициализирует ML-модель (Flux), генерирует изображение.
3.  **Storage**: Результат генерации загружается в S3-хранилище.
4.  **Result**: Worker обновляет статус задачи в БД на `COMPLETED` и сохраняет ссылку на файл.

---

## 🚀 Установка и Запуск (Dev)

### Предварительные требования
*   Docker и Docker Compose
*   NVIDIA GPU (рекомендуется для Flux, но можно запустить на CPU - будет медленно)

### Шаг 1: Клонирование и Env
```bash
git clone <repo_url>
cd <repo_name>
cp .env.example .env
```
Отредактируйте `.env` при необходимости.

### Шаг 2: Запуск через Docker Compose
Поднимет базу данных, Redis, MinIO (S3), API и Worker.
```bash
docker-compose up --build -d
```

Сервис будет доступен по адресу: `http://localhost:80` (через Nginx) или `http://localhost:8000` (напрямую).
Документация Swagger: `http://localhost:80/docs`

---

## 🗄 Миграции БД

Миграции управляются через Alembic. При первом запуске они могут примениться автоматически (если настроено в entrypoint), но для ручного управления:

```bash
# Применить все миграции
docker-compose exec backend alembic upgrade head

# Создать новую миграцию (при изменении models.py)
docker-compose exec backend alembic revision --autogenerate -m "Description"
```

---

## 🔌 API Интерфейс

Для всех запросов требуется заголовок авторизации:
`X-API-Key: your_secret_key_from_env`

### 1. Создать задачу генерации
**POST** `/v1/generate`

```json
{
  "provider": "flux",
  "prompt": "A futuristic cyberpunk cat neon city",
  "width": 1024,
  "height": 1024,
  "num_images": 1
}
```

**Ответ:**
```json
{
  "task_id": "a1b2c3d4-...",
  "status": "queued",
  "provider": "flux",
  "created_at": "2023-10-27T10:00:00Z"
}
```

### 2. Получить статус задачи
**GET** `/v1/tasks/{task_id}`

**Ответ (Completed):**
```json
{
  "task_id": "a1b2c3d4-...",
  "status": "completed",
  "provider": "flux",
  "images": [
    {
      "url": "http://localhost:9000/images/generated/...",
      "s3_key": "generated/..."
    }
  ],
  "params": { ... }
}
```

---

## ⚙️ Конфигурация (.env)

Ключевые переменные:

*   **APP_ENV**: `dev` или `prod`
*   **API_KEY**: Секретный ключ для доступа к API.
*   **DATABASE_URL**: Строка подключения к Postgres.
*   **S3_ENDPOINT_URL**: URL S3 хранилища.
*   **FLUX_MODEL_ID**: `black-forest-labs/FLUX.1-schnell` (по умолчанию).

Полный список см. в `.env.example`.

---

## 📦 Деплой

Проект настроен для CI/CD через GitLab CI (`.gitlab-ci.yml`).

1.  **Build**: Собирает Docker-образ.
2.  **Test**: Запускает Pytest.
3.  **Deploy Test**: Автоматически при пуше в `develop`.
4.  **Deploy Prod**: Ручной запуск при создании тега.

Для продакшена убедитесь, что `docker-compose.yml` использует корректные вольюмы и драйверы GPU.
